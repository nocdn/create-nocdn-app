# {{project-name}}

To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

The API runs on port `{{port}}` by default and still respects `PORT` if you want to override it.

Open http://localhost:{{port}}
